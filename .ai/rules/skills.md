---
paths:
  - '.claude/skills/**'
  - '.ai/skills/**'
  - '.ai/rules/**'
---

# Skills

## Project-authored skills live in .ai/skills, never .claude/skills
`php artisan boost:update` rewrites `.claude/skills/*/SKILL.md` from whatever it
discovers, so a project-authored skill parked there gets silently replaced by the
vendor version — `pest-testing/SKILL.md` was clobbered that way once (the stock
Pest skill ships inside `pestphp/pest`, and because Pest is one of Boost's
first-party packages nothing in `boost.json` can opt out of it).

The fix is structural, not a review ritual: **our copy lives in
`.ai/skills/<name>/`**. Boost merges `.ai/skills/*` last as custom user skills, so
ours wins, and it writes `.claude/skills/<name>` as a symlink to it
(`pest-testing` and `deploying-laravel-cloud` are both symlinks today). Edit the
`.ai/` file; never the symlink target path under `.claude/`.

**Commit a skill move in the same breath as making it.** While a move is still
uncommitted, `git status` reports the *old* path (`.claude/skills/<name>/SKILL.md`)
while a symlink already sits at `.claude/skills/<name>` — and git refuses any
pathspec that traverses a symlink (`fatal: … is beyond a symbolic link`, exit 128).
Pest's Tia plugin feeds `git status` output straight into `git check-ignore`, reads
that 128 as "git is missing", and kills the run: `composer test:unit` dies with
`The feature "Tia mode" requires "git"` before a single test executes. Committing
clears it (git then knows only the `.ai/` path plus the symlink). If you are stuck
mid-move, run the gate with `--no-tia` or delete the symlink until you commit.

Do **not** try to solve this with `config('boost.skills.exclude')` instead. A name
excluded from discovery while still listed in `boost.json`'s `skills` array counts
as *stale*, and `SkillWriter::sync()` deletes the directory outright.

Anything still under `.claude/skills/` (`laravel-best-practices`,
`filament-compass`, `infer-conventions`, …) is Boost-rendered output from a
package's `*.blade.php`. Take upstream refreshes as-is; if you ever need to change
one, move it to `.ai/skills/` first.

## .ai/rules/index.md and .ai/rules/boost/** are generated — don't hand-edit
`RuleRepository::writeIndex()` rewrites `index.md` in full on every `record-rule`
call, and `.ai/rules/boost/**` is regenerated by `boost:install`/`boost:update`
from the installed packages' guidelines (it exists because
`BOOST_RULES_SCOPED_GUIDELINES=true` — keep that set, or those path-scoped
guidelines silently fall back to being inlined in `CLAUDE.md`). Hand edits to
either are lost on the next run.

Write rules only in the hand-authored `.ai/rules/*.md` files. Note that a glob can
land in two files — `app/Models/**` and `tests/**` each have both ours and a
`boost/` one — which is expected: read both, and keep our file to what the
generated one doesn't say.

## Watch for upstream advice that contradicts this project
The stock Pest skill tells you to use `RefreshDatabase`, which is wrong here:
tenancy writes on a separate connection under the RLS role, so a transaction on
the superuser connection neither covers nor rolls it back. `tests/Pest.php` owns
the database lifecycle. When grafting upstream material into one of our skills,
re-check it against the project before keeping it.
